<div class="header">
	<h1 key="loved">{{L 'loved'}}</h1>
	<hr>
</div>
<div class="half">
	<div class="padding">
		<table>
			{{#each songs}}
				{{> song}}
			{{/each}}
			{{#empty songs}}
				<tr><td class="empty">{{L 'emptyLoved'}}</td>
			{{/empty}}
		</table>
	</div>
</div>
